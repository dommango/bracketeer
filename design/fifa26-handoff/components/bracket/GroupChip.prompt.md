A letter tile for one of the 12 World Cup groups (A–L). Each group is assigned a host-city color so the bracket reads as a rainbow when zoomed out.

```jsx
<GroupChip group="A" />
<GroupChip group="F" size="lg" />
```
